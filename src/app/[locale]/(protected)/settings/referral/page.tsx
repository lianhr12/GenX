import ReferralPageClient from '@/components/settings/referral/referral-page-client';

/**
 * Referral page, show referral code, link, stats and history
 */
export default function ReferralPage() {
  return <ReferralPageClient />;
}
